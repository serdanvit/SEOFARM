# SEO FARM — зависимости
# Работает на Python 3.8 – 3.14+
# НЕТ бинарных пакетов — устанавливается на любой версии Python

flask==3.0.3
flask-cors==4.0.1
requests==2.31.0
werkzeug==3.0.3

# PostgreSQL (раскомментируй если нужен PostgreSQL):
# psycopg2-binary==2.9.9
